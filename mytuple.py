# Tạo tuple
# Tuple giống list nhưng không thể thay đổi sau khi tạo.
numbers = (1, 2, 3, 4)

# Truy cập phần tử
print(numbers[0])  # 1

# Duyệt tuple bằng vòng lặp
for num in numbers:
    print(num)
